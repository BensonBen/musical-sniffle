/**
 * Contains x and y coordinates of the corresponding theta.
 * Also, these x and y coordinates are in relation to the way a monitor draws graphics.
 */
export interface ThetaMetadata {
  x: number;
  y: number;
  theta: number;
}
